import { Button } from "@mui/material"

export default function TestComponent(){
    return(
        <Button
            type="submit"
            variant="contained"
            color="primary"
        >Hello</Button>
    )
}